export const selectVehicleType = state => state.campers.vehicleType;
export const selectCampers = state => state.campers.campers;
export const selectFilters = state => state.campers.equipmentFilters;
export const selectLocation = state => state.campers.location;
export const selectPagination = state => state.campers.pagination;
